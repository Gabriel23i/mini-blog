import styled from 'styled-components';

export const Container = styled.div `
    text-align: center;
    margin-top: 100px;

    h4 {
        margin-bottom: 2rem;
    };

    p {
        margin: 1.8rem 0 0.5rem 0;
        color: #000;
        font-weight: 500;
    };

    img {
        max-width: 100%;
        margin-bottom: 1em;
    };
`;
